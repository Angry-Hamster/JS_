{{!-- библиотека принимает массив объектов к перебору
в this хранит итерируемый объект массива
и раструшивает свойтсва каждого объекта из массива
в прописанные нами элементы --}}
{{#each this}}
<li>
  <div>
    <img src="{{author.avatar}}" alt="{{author.name}}">
    <p>{{author.name}}</p>
  </div>
  <p>{{text}}</p>
  <p>{{creatAt}}</p>
  <ul>
    {{#each tags}}
    <li>{{this}}</li>
    {{/each}}
  </ul>
</li>
{{/each}}